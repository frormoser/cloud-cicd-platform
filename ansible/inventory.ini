; Example Ansible inventory for local/dev usage
[jenkins]
jenkins.example.com ansible_user=ec2-user

[k8s_nodes]
node1.example.com ansible_user=ec2-user

# Note: This is a demo inventory. Replace hosts with real IPs or use dynamic
# inventory (AWS EC2 plugin) for production.
